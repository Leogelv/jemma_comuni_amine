/**
 * DESIGN TOKENS — GLASSMORPHISM ДИЗАЙН-СИСТЕМА
 *
 * Унифицированная система токенов для Anti Self-Deception.
 * Стиль: Glassmorphism (вдохновлено macOS/iOS, currencyconv проект).
 *
 * Использование: импортируй токены и применяй как строки Tailwind классов.
 */

export const designTokens = {
  // ========================================
  // ЦВЕТА: Текст
  // ========================================
  colors: {
    text: {
      primary: 'text-white/95',      // Основной текст (высокий контраст)
      secondary: 'text-slate-400',   // Вторичный текст (описания, метки)
      accent: 'text-emerald-400',    // Акценты (успех, прогресс)
      danger: 'text-red-400',        // Ошибки, предупреждения
      muted: 'text-slate-600',       // Приглушённый текст (disabled состояния)
    },
    bg: {
      // Градиентный фон для всего приложения
      gradient: 'bg-gradient-to-br from-[#090B13] via-[#141727] to-[#1E2240]',
    },
  },

  // ========================================
  // MATERIALS: Glassmorphism эффекты
  // ========================================
  materials: {
    // Стеклянная поверхность (базовое состояние)
    glass: 'bg-white/5 backdrop-blur-2xl border border-white/10',

    // Состояние hover (светлее и ярче граница)
    glassHover: 'hover:bg-white/10 hover:border-white/20',

    // Активное состояние (нажатие, выбрано)
    glassActive: 'bg-white/10 border-white/20',

    // Комбинация: glass + hover (используй в интерактивных элементах)
    glassInteractive: 'bg-white/5 backdrop-blur-2xl border border-white/10 hover:bg-white/10 hover:border-white/20 transition-all duration-200',
  },

  // ========================================
  // СКРУГЛЕНИЯ
  // ========================================
  radius: {
    sm: 'rounded-xl',        // 12px — мелкие элементы (badge, чипы)
    md: 'rounded-2xl',       // 16px — карточки, кнопки
    lg: 'rounded-[28px]',    // 28px — большие контейнеры, модальные окна
    full: 'rounded-full',    // 9999px — круглые кнопки, аватары
  },

  // ========================================
  // КАТЕГОРИИ: Glassmorphism стиль
  // ========================================
  categories: {
    sport: {
      // Оранжевый (энергия, активность)
      color: 'bg-orange-500/20',           // Полупрозрачный фон
      border: 'border-orange-500/30',      // Граница с прозрачностью
      text: 'text-orange-400',             // Яркий текст
      colorHex: '#F97316',                 // Hex для inline стилей (если нужно)
      emoji: '💪',
      label: 'Спорт',
      points: 10,
    },
    nutrition: {
      // Бирюзовый (свежесть, здоровье)
      color: 'bg-teal-500/20',
      border: 'border-teal-500/30',
      text: 'text-teal-400',
      colorHex: '#14B8A6',
      emoji: '🥗',
      label: 'Питание',
      points: 8,
    },
    regime: {
      // Фиолетовый (дисциплина, режим)
      color: 'bg-violet-500/20',
      border: 'border-violet-500/30',
      text: 'text-violet-400',
      colorHex: '#8B5CF6',
      emoji: '⏰',
      label: 'Режим',
      points: 12,
    },
    other: {
      // Серый (нейтральный)
      color: 'bg-slate-500/20',
      border: 'border-slate-500/30',
      text: 'text-slate-400',
      colorHex: '#64748B',
      emoji: '✨',
      label: 'Другое',
      points: 5,
    },
  },

  // ========================================
  // ТЕНИ (для стеклянных элементов)
  // ========================================
  shadows: {
    glass: 'shadow-lg shadow-black/20',      // Стандартная тень для glass-карточек
    glassHover: 'shadow-xl shadow-black/30', // Поднятая тень при hover
    none: 'shadow-none',
  },

  // ========================================
  // АНИМАЦИИ (Transitions)
  // ========================================
  transitions: {
    fast: 'transition-all duration-150 ease-out',     // Быстрые микро-взаимодействия
    base: 'transition-all duration-200 ease-out',     // Стандартные переходы
    slow: 'transition-all duration-300 ease-out',     // Медленные (модальные окна)
    spring: 'transition-all duration-400 cubic-bezier(0.34, 1.56, 0.64, 1)', // Spring-эффект
  },
} as const;

// ========================================
// ТИПЫ (для TypeScript автокомплита)
// ========================================
export type CategoryType = keyof typeof designTokens.categories;

/**
 * Хелпер: получить конфигурацию категории по ID
 */
export function getCategoryTokens(categoryId: CategoryType | string) {
  return designTokens.categories[categoryId as CategoryType] || designTokens.categories.other;
}
